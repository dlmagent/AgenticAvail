services:
  backend:
    build: ./backend
    env_file: [.env]
    ports: ["8000:8000"]

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: http://127.0.0.1:8000
    ports: ["3000:80"]
    depends_on: [backend]
